version: '3.4'

services:

  auctionDb:
    image: postgres

  mongoDb:
    image: mongo

  auctionservice:
    image: ${DOCKER_REGISTRY-}auctionservice
    build:
      context: .
      dockerfile: src/AuctionService/Dockerfile


 
volumes:
  postgres_auction:
  mongo_data:

